{
  "mcpServers": {
    "rewatch": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "${workspaceFolder}/mcp_server", "rewatch-mcp"],
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
