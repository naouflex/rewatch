{
  "mcpServers": {
    "rewatch": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "${workspaceFolder}/mcp_server", "rewatch-mcp"],
      "env": {
        "REWATCH_BASE_URL": "https://rewatch.naoufel.io"
      }
    }
  }
}
