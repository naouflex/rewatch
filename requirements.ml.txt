# MLModels feature — heavy ML stack installed only in the dedicated
# `rewatch-ml-worker` image (see Dockerfile.ml). Pinned to versions compatible
# with Python 3.13 and numpy 2.x. We intentionally do NOT depend on
# tensorflow/keras: the MLModel runtime is scikit-learn-only.
scikit-learn==1.7.2
joblib==1.5.3
scipy==1.14.1
statsmodels==0.14.4
